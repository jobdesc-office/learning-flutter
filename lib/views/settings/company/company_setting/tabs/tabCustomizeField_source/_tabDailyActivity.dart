part of '../../company.dart';

class _TabCustomizeFieldDailyActivity extends StatelessWidget {
  const _TabCustomizeFieldDailyActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
